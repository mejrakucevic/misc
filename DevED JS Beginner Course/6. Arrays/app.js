const schedule = ['Wake up', 'Eat', 'Film Videos'];

// schedule.push('Hey a new thing got added'); add a new value

// schedule.pop(); remove the last from the array

// schedule.shift(); remove the first from the array

// console.log(schedule[2]);

console.log(schedule.indexOf('Eat')); // shows the number of the value

const film = schedule.indexOf('Film Videos');

console.log(film);

