const name = "Mejra";

const prezime = "Kucevic";

function adder(num1, num2) {
    // console.log('Your Number is')
    console.log(num1 - num2)

}

adder(10, 6)

adder();

function toUpper(text) {
    const upperCased = text.toUpperCase();
    console.log(upperCased)
}

toUpper(name)
toUpper(prezime)

const toUpperr = (text) =>{
    const gore = text.toUpperCase();
    console.log(gore )

}


toUpperr(name)
